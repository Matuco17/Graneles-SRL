<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:ui="http://java.sun.com/jsf/facelets"
      xmlns:h="http://java.sun.com/jsf/html"
      xmlns:f="http://java.sun.com/jsf/core"
      xmlns:p="http://primefaces.prime.com.tr/ui">

    <f:metadata>  
        <f:event type="preRenderView" listener="#{muelleController.init()}" />  
    </f:metadata> 


    <ui:composition template="/template.xhtml">
        <ui:define name="title">
            <h:outputText value="#{bundleCarga.ListMuelleTitle}"></h:outputText>
        </ui:define>
        <ui:define name="body">
            <h:form styleClass="jsfcrud_list_form">
                <h:outputText escape="false" value="#{bundleCarga.ListMuelleEmpty}" rendered="#{muelleController.items.rowCount == 0}"/>
                <h:panelGroup rendered="#{muelleController.items.rowCount > 0}">
                    <p:dataTable value="#{muelleController.items}" 
                                 var="item"
                                 paginator="true" rows="10"
                                 paginatorTemplate="{CurrentPageReport}  {FirstPageLink} {PreviousPageLink} {PageLinks} {NextPageLink} {LastPageLink} {RowsPerPageDropdown}"
                                 rowsPerPageTemplate="5,10,15">
                        <p:column>
                            <f:facet name="header">
                                <h:outputText value="#{bundleCarga.MuelleTitle_descripcion}"/>
                            </f:facet>
                            <h:outputText value="#{item.descripcion}"/>
                        </p:column>
                        <p:column>
                            <f:facet name="header">
                                <h:outputText value="#{bundleCarga.MuelleTitle_sitio}"/>
                            </f:facet>
                            <h:outputText value="#{item.sitio}"/>
                        </p:column>
                        <p:column>
                            <f:facet name="header">
                                <h:outputText value="#{bundleCarga.MuelleTitle_puerto}"/>
                            </f:facet>
                            <h:outputText value="#{item.puerto.toString()}"/>
                        </p:column>
                        <p:column>
                            <f:facet name="header">
                                <h:outputText value="&nbsp;"/>
                            </f:facet>
                            <h:commandButton 
                                image="/faces/resources/images/view.png" 
                                action="#{muelleController.prepareView}" 
                                value="#{bundleCarga.MuelleViewLink}"
                                title="#{bundleCarga.MuelleViewLink}"
                                />

                            <h:commandButton 
                                image="/faces/resources/images/edit.png" 
                                action="#{muelleController.prepareEdit}" 
                                value="#{bundleCarga.MuelleEditLink}"
                                title="#{bundleCarga.MuelleEditLink}"
                                />

                            <h:commandButton 
                                image="/faces/resources/images/destroy.png"
                                onclick="return confirm('¿Esta seguro que desea eliminar este Muelle?');" 
                                action="#{muelleController.destroy}" 
                                title="#{bundleCarga.MuelleDestroyLink}"
                                value="#{bundleCarga.MuelleDestroyLink}"
                                />
                        </p:column>
                    </p:dataTable>
                </h:panelGroup>
                <h:commandButton 
                    image="/faces/resources/images/new.png" 
                    action="#{muelleController.prepareCreate}" 
                    value="#{bundleCarga.MuelleCreateLink}"
                    title="#{bundleCarga.MuelleCreateLink}"
                    />
            </h:form>
        </ui:define>
    </ui:composition>

</html>
